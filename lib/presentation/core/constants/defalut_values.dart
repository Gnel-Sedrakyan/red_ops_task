import 'package:flutter/material.dart';

const defaultPadding = 30.0;
const defaultSpacing = 10.0;
const defaultBorderRadius = 10.0;

const defaultButtonShadowColor = Color.fromRGBO(120, 120, 120, 0.5);
const defaultButtonShadowOffset = Offset(25, 24);
const defaultButtonShadowSpreadRadius = 1.0;
const defaultButtonShadowBlurRadius = 35.0;

const defaultShadowColor = Color.fromRGBO(77, 77, 119, 0.2);
const defaultShadowOffset = Offset(25, 24);
const defaultShadowSpreadRadius = 0.0;
const defaultShadowBlurRadius = 35.0;

const primaryShadowColor = Color.fromARGB(127, 78, 188, 27);
const secondaryShadowColor = Color.fromARGB(127, 237, 151, 1);

const transitionDuration = Duration(milliseconds: 700);
const transitionShortDuration = Duration(milliseconds: 100);
